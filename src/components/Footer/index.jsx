import { EMAIL } from 'helpers'
import { Main, Nav } from './style'

export const Footer = () => (
  <div>
    <Nav>
      <Main> For Contact </Main>
      <Main> {EMAIL} </Main>
    </Nav>
  </div>
)
